import StateContainer from './StateContainer';
import State from './State';
export default class HistoryStateContainer extends StateContainer {
    private maxHistory;
    private history;
    private redoStates;
    constructor(initialState?: State, maxHistory?: number, debug?: boolean);
    undo(): void;
    redo(): void;
    protected updateState(newState: State, keepRedos?: boolean): void;
    historySize: number;
    redoSize: number;
}
