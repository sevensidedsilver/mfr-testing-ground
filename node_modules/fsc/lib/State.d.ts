export default class State {
    [key: string]: any;
    previousState: State;
    changedKeys: Array<string>;
    constructor(state?: {
        [key: string]: any;
    });
    clone(): State;
}
